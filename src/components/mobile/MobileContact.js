import React, { useContext } from "react";
import { ThemeContext } from "../../context"

import "./MobileContact.css";

const MobileContact = () => {
  return (
    <div id="mobile-contact" className="mobile-contact">
      <p>Contact</p>
    </div>
  );
};

export default MobileContact;
